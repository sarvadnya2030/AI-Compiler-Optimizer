# Frontend

React + Vite dashboard for the AI Compiler Optimizer. See the
[project README](../README.md) for the full picture.

```bash
npm install
npm run dev
```

Configure the backend URL via `VITE_API_BASE_URL` in `.env` (defaults to
`http://localhost:8000`).
